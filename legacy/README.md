# Legacy Prototypes

This directory keeps early one-off scripts used while developing the SIFT
matching pipeline.

These files are useful for understanding the project history, but the current
runtime path is the package-based implementation under `sau_sift_nav/`.

Current entry points:

```text
live_sift_nav.py
sift_dashboard_launcher.py
```

